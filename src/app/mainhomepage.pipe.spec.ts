import { MainhomepagePipe } from './mainhomepage.pipe';

describe('MainhomepagePipe', () => {
  it('create an instance', () => {
    const pipe = new MainhomepagePipe();
    expect(pipe).toBeTruthy();
  });
});
